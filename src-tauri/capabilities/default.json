{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Local application window; no remote origins receive native access",
  "windows": ["main"],
  "permissions": ["core:default"]
}
